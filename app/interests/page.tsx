"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{AppShell}from"@/components/AppShell";import{interests}from"@/lib/data";
export default function Interests(){const[chosen,setChosen]=useState<string[]>([]),router=useRouter();const done=()=>{localStorage.setItem("kpop-interests",JSON.stringify(chosen));fetch("/api/analytics",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({eventName:"interest_selected",properties:{count:chosen.length}})}).catch(()=>{});router.push("/home")};return <AppShell plain><div className="onboard"><p className="eyebrow">让首页更懂你</p><h1 className="page-title">选几个你喜欢的吧 ♡</h1><p className="lead">组合、Solo 艺人或成员都可以，以后还能修改。</p><div className="chips">{interests.map(i=><button key={i} className={`chip ${chosen.includes(i)?"on":""}`} onClick={()=>setChosen(x=>x.includes(i)?x.filter(y=>y!==i):[...x,i])}>{i}</button>)}</div><button className="primary wide" style={{marginTop:35}} onClick={done}>{chosen.length?`选好了（${chosen.length}）`:"以后再选"}</button></div></AppShell>}
