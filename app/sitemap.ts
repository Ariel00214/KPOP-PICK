import type{MetadataRoute}from"next";import{albums}from"@/lib/data";export default function sitemap():MetadataRoute.Sitemap{const base="https://myarea.website";return["","/home","/search","/assistant","/login","/privacy","/terms",...albums.map(a=>`/album/${a.id}`)].map(url=>({url:`${base}${url}`,lastModified:new Date(),changeFrequency:url.startsWith("/album")?"daily":"weekly",priority:url===""?1:.7}))}
