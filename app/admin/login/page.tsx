"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import{AppShell}from"@/components/AppShell";
export default function AdminLogin(){const[pw,setPw]=useState(""),[error,setError]=useState(""),router=useRouter();const login=async()=>{setError("");const r=await fetch("/api/admin/auth",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password:pw})});if(r.ok){router.replace("/admin/analytics");router.refresh()}else setError("密码不正确")};return <AppShell plain><div className="admin-lock"><div className="logo-big">KPOP PICK</div><h1 className="page-title">MVP DATA</h1><p className="lead">管理员数据看板</p><div className="field"><input type="password" value={pw} onChange={e=>setPw(e.target.value)} onKeyDown={e=>e.key==="Enter"&&login()} placeholder="管理员密码"/></div>{error&&<div className="warn">{error}</div>}<button className="primary wide" style={{marginTop:12}} onClick={login}>进入</button></div></AppShell>}
