"use client";import{useEffect,useState}from"react";import Link from"next/link";import{AppShell}from"@/components/AppShell";
type Row={id:string;extractedData:string;verificationError?:string;updatedAt:string};export default function Reports(){const[rows,setRows]=useState<Row[]>([]);const load=()=>fetch("/api/admin/submissions").then(r=>r.ok?r.json():Promise.reject()).then(body=>setRows(body.data||[])).catch(()=>location.href="/admin/login");useEffect(()=>{void load()},[]);return <AppShell plain><h1>待补证线索</h1><p className="lead">系统尚未找到完整官方证据；无需管理员手动搜索，可稍后重新核实。</p><div className="stack">{rows.map(row=>{const data=JSON.parse(row.extractedData||"{}");return <article className="card form-card" key={row.id}><h3>{data.artistName||"未知艺人"}</h3><p>{data.clue||data.sourceUrl||"截图线索"}</p><div className="warn">{row.verificationError}</div><button className="secondary wide" onClick={async()=>{await fetch("/api/admin/submissions",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({id:row.id})});void load()}}>重新核实</button></article>})}{!rows.length&&<div className="empty card">暂无待补证线索</div>}</div><Link href="/admin/releases" className="primary wide" style={{display:"block",textAlign:"center",marginTop:16}}>进入回归审核</Link></AppShell>}
