"use client";import{useMemo,useState}from"react";import{AppShell}from"@/components/AppShell";import{photocards}from"@/lib/data";
export default function Photocards(){const[type,setType]=useState("全部");const types=["全部","预购特典","店铺特典","专辑内随机卡","Lucky Draw"];const list=useMemo(()=>type==="全部"?photocards:photocards.filter(p=>p.type.includes(type)),[type]);return <AppShell><h1 className="page-title">找小卡</h1><p className="lead">按成员、专辑和小卡类型快速筛选。</p><div className="filterbar">{types.map(t=><button className={`chip ${type===t?"on":""}`} onClick={()=>setType(t)} key={t}>{t}</button>)}</div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:14}}>{list.map(p=><article className="pcard" style={{width:"100%"}} key={p.id}><div className="pc-art" style={{height:190,background:p.color}}>{p.initials}</div><strong>{p.member}</strong><span>{p.album}</span><span>{p.type} · {p.random?"随机":"可指定"}</span></article>)}</div><p className="footnote">小卡图为 DEMO 占位视觉，不对应真实商品。</p></AppShell>}
