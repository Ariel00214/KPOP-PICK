"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import{AppShell}from"@/components/AppShell";import{Track}from"@/components/Track";import type{Mode}from"@/lib/types";
export default function Onboarding(){const[mode,setMode]=useState<Mode>();const router=useRouter();const go=()=>{if(!mode)return;localStorage.setItem("kpop-mode",mode);fetch("/api/analytics",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({eventName:"mode_selected",properties:{mode}})}).catch(()=>{});router.push("/interests")};return <AppShell plain><Track eventName="app_open"/><div className="onboard center"><div className="logo-big">KPOP PICK</div><p className="eyebrow">专辑 · 小卡 · 全平台购买信息</p><h1 className="page-title" style={{marginTop:45}}>你平时怎么买 K-pop 专辑？</h1></div><div className="stack section"><button className={`mode-card card ${mode==="beginner"?"selected":""}`} onClick={()=>setMode("beginner")}><div className="emoji">🌱</div><h2>第一次买 / 还不太熟悉</h2><p>不懂版本和特典也没关系，一步一步帮你找到适合自己的购买方式。</p></button><button className={`mode-card card ${mode==="expert"?"selected":""}`} onClick={()=>setMode("expert")}><div className="emoji">🎴</div><h2>已经比较熟悉怎么买专</h2><p>直接查看版本、小卡、价格和渠道，快速筛选购买方案。</p></button></div><button className="primary wide" disabled={!mode} onClick={go}>继续</button><p className="skip">以后可以随时切换模式</p></AppShell>}
