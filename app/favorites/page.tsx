"use client";import{useEffect,useState}from"react";import{AppShell}from"@/components/AppShell";import{AlbumCard}from"@/components/AlbumCard";import{albums}from"@/lib/data";
export default function Favorites(){const[ids,setIds]=useState<string[]>([]);useEffect(()=>setIds(JSON.parse(localStorage.getItem("kpop-favorites")||"[]")),[]);const shown=albums.filter(a=>ids.includes(a.id));return <AppShell><h1 className="page-title">收藏</h1>{shown.length?<div className="scroll section">{shown.map(a=><AlbumCard key={a.id} album={a}/>)}</div>:<div className="empty card section">还没有收藏<br/><span className="eyebrow">在专辑页收藏想继续关注的回归</span></div>}</AppShell>}
