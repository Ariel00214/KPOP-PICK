"use client";import{useState,useMemo}from"react";import Link from"next/link";import{Search,ChevronRight}from"lucide-react";import{AppShell}from"@/components/AppShell";import{useCatalog}from"@/lib/use-catalog";import{formatMoney}from"@/lib/money";
export default function SearchPage(){const[q,setQ]=useState(""),albums=useCatalog();const list=useMemo(()=>albums.filter(a=>`${a.artist} ${a.title}`.toLowerCase().includes(q.toLowerCase())),[q,albums]);const track=()=>fetch("/api/analytics",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({eventName:"search_performed",properties:{queryLength:q.length,resultCount:list.length}})}).catch(()=>{});return <AppShell><h1 className="page-title">找专辑</h1><div className="search"><Search size={18}/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} onBlur={track} placeholder="艺人 / 专辑原名"/></div><div className="stack section">{list.length?list.map(a=><Link className="result card" href={`/album/${a.id}`} key={a.id}><div className="mini-cover" style={{background:a.accent}}>{a.cover}</div><div><h3>{a.artist}</h3><p>{a.title}</p><p>{a.channelCount} 个渠道 · {a.lowestPrice===null?"暂无在售价格":`${formatMoney(a.lowestPrice,a.offers[0]?.currency)} 起`}</p></div><ChevronRight className="arrow" size={19}/></Link>):<div className="empty card">没有找到相关专辑<br/>试试艺人名或专辑原名</div>}</div></AppShell>}
