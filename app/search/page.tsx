"use client";import{useState,useMemo}from"react";import Link from"next/link";import{Search,ChevronRight}from"lucide-react";import{AppShell}from"@/components/AppShell";import{albums}from"@/lib/data";
export default function SearchPage(){const[q,setQ]=useState("");const list=useMemo(()=>albums.filter(a=>`${a.artist} ${a.title} ${a.titleZh}`.toLowerCase().includes(q.toLowerCase())),[q]);const track=()=>fetch("/api/analytics",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({eventName:"search_performed",properties:{queryLength:q.length,resultCount:list.length}})}).catch(()=>{});return <AppShell><h1 className="page-title">找专辑</h1><div className="search"><Search size={18}/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} onBlur={track} placeholder="组合 / 成员 / 专辑"/></div><div className="stack section">{list.length?list.map(a=><Link className="result card" href={`/album/${a.id}`} key={a.id}><div className="mini-cover" style={{background:a.accent}}>{a.cover}</div><div><h3>{a.artist}</h3><p>{a.title}</p><p>{a.channelCount} 个渠道 · ¥{a.lowestPrice} 起</p></div><ChevronRight className="arrow" size={19}/></Link>):<div className="empty card">没有找到相关专辑<br/>试试艺人名或专辑原名</div>}</div></AppShell>}
